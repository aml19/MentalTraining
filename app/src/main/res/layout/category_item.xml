<?xml version="1.0" encoding="utf-8"?>
<!--
    -This class lays the Category elements in a vertical line to be scrolled
        -Width: Matches Parent
        -Height: hardcoded to look good.
            -might dynamically change height based on number of inputs in Java/source
    -Items should be added dynamically in Java/ folder as a Array<String>
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <FrameLayout

        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/categoryText"
            android:layout_width="match_parent"
            android:layout_height="68dp"
            android:text="TextView"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"/>
    </FrameLayout>
</LinearLayout>
